import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ArbitroresgisPage } from './arbitroresgis.page';

describe('ArbitroresgisPage', () => {
  let component: ArbitroresgisPage;
  let fixture: ComponentFixture<ArbitroresgisPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ArbitroresgisPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
